import styled from 'styled-components';

export const IconsWrapper = styled.div`
    display: flex;
    gap: 12px;
    align-items: center;

    svg {
        font-size: 28px;
        color: #cbb4ff;
        
    }
`